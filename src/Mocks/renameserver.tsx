// import { setupServer } from "msw/node";
// import { failureHandler, successHandler } from "./handlers";

// export const server = setupServer(successHandler, failureHandler);
// beforeAll(() => server.listen());
// afterEach(() => server.resetHandlers());
// afterAll(() => server.close());
